#:kivy 1.8.0
#:import evolve kivy_gol_bindings.evolve
#:import DrawableGrid kivy_grid_cells

<DrawableGrid>:
    size: self.cols_adjusted, self.rows_adjusted

<GridCell>:
    canvas:
        Color:
            rgba: self.colour
        Rectangle:
            pos: self.pos
            size: self.size

CustomLayout:
    grid: grid
    orientation: "vertical"

    AnchorLayout:
        size_hint: 1, 1
        anchor_x: "left"
        anchor_y: "bottom"

        DrawableGrid:
            id: grid
            size_hint: 0, 0
            rows: 30
            cols: 30
            cell_size: 15

    AnchorLayout:
        size_hint: 1, 1
        anchor_x: "right"
        anchor_y: "bottom"
        size_hint: 1, .2

        Button:
            id: next_button
            text: "Next"
            size_hint: 0, 0
            size: 100, 50
            on_press: evolve(grid)
